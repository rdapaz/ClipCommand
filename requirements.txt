# ── Core ──────────────────────────────────────────────────────────────────────
# Cross-platform clipboard access (required)
pyperclip>=1.8.2

# ── Word transform dependencies ───────────────────────────────────────────────

# Windows: COM automation for live Word document access
# (also provides win32com used by word_utils.py on Windows)
pywin32>=311; sys_platform == "win32"

# macOS: no extra packages needed — word_utils.py uses osascript (built-in)

# Linux / cross-platform file-based fallback
# Also useful on macOS/Windows if you prefer editing files directly
python-docx>=1.1.0

# YAML parsing — required for word_from_yaml* transforms
PyYAML>=6.0.2

# ── Optional ──────────────────────────────────────────────────────────────────

# Hotkey support (--hotkey flag)
# Linux note: also requires sudo or uinput group membership
# keyboard>=0.13.5
